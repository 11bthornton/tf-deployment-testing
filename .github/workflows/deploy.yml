name: Deploy via Terraform

on:
  workflow_dispatch:

jobs:
  dev:
    name: 'Deploy to Dev'
    uses: ./.github/workflows/deploy.template.yml

  uat:
    name: 'Deploy to UAT'
    uses: ./.github/workflows/deploy.template.yml