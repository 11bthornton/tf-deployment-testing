name: Main Deploy

on:
  workflow_dispatch:

jobs:
  dev:
    name: 'Deploy to Dev'
    uses: ./.github/workflows/deploy.template.yml

  uat:
    needs: dev
    name: 'Deploy to UAT'
    uses: ./.github/workflows/deploy.template.yml