name: Main Deploy

on:
  workflow_dispatch:

jobs:
  dev:
    name: 'Dev'
    uses: ./.github/workflows/deploy.template.yml

  uat:
    needs: dev
    name: 'UAT'
    uses: ./.github/workflows/deploy.template.yml